*// What-is-P (2026-2020)
*// Contains Edited MIT and severe data loading









// yes.

yes.

// yes.
































































[RECTV] RECEIVED_DATA
[RECTO] RECEIVED_RENDER

// cube the RecTv/Recv

[RECV] CUBED_DATA
[RECO] SQUARED_DATA

if recv = nil.brazil(Network.lambda(=1))

   return nnd(smcamma)
   return mmd(smcamma)
   return ver(1.camma)
   return camma(1.25.camma, 1.yr.camma)

if loadstring(noop) = true

    return RECV³
    return RECV¹
    return RECO³
    return RECO¹

RECV/RECO = return(DATA)

// receive the data

recieve DATA = true

return(DATA)

DATA(0x000, bin, all, random)

return all, bin, random, 0x000

    var

loadstring(data)

// return binary values

return 0
return 1

// now we have some data received!